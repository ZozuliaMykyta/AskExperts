<?php
    // use PHPMailer\PHPMailer\PHPMailer;
    // use PHPMailer\PHPMailer\Exception;

    // require 'phpmailer/src/Exception.php';
    // require 'phpmailer/src/PHPMailer.php';

    // $mail = new PHPMailer(true);
    // $mail->CharSet = 'UTF-8';
    // $mail->setLanguage('ru', 'phpmailer/language/');
    // $mail->isHTML(true);

    // $mail->setFrom('', 'I want to contact');
    // $mail->addAddress('');
    // $mail->Subject = 'I want to contact';

    // $body = '<h1>I want to contact</h1>';

    // if(trim(!empty($_POST['username']))){
    //     $body.='<p><strong>name:</strong> '.$_POST['username'].'</p>';
    // }
    // if(trim(!empty($_POST['tel']))){
    //     $body.='<p><strong>tel:</strong> '.$_POST['tel'].'</p>';
    // }
    // if(trim(!empty($_POST['company']))){
    //     $body.='<p><strong>company:</strong> '.$_POST['company'].'</p>';
    // }
    // if(trim(!empty($_POST['email']))){
    //     $body.='<p><strong>email:</strong> '.$_POST['email'].'</p>';
    // }
    // if(trim(!empty($_POST['message']))){
    //     $body.='<p><strong>message:</strong> '.$_POST['message'].'</p>';
    // }

    // $mail->Body = $body;

    // if(!$mail->send()){
    //     $message = "Error";
    // } else{
    //     $message = "Data has been sent";
    // }

    // $response = ['message' => $message];

    // header('Content-type: application/json');
    // echo json_encode($response);
